# Vibe Design

Vibe Design - это инструмент для автоматизации работы с дизайном в Figma, который использует AI (LLM) и MCP (Model Context Protocol) для анализа и редактирования дизайна.

## Основные возможности

- 🎨 Извлечение данных из Figma (структура, дизайн-токены)
- 🤖 AI-ассистент для генерации и редактирования компонентов
- 🔄 Автоматическое обновление дизайна через Figma Plugin
- 📊 Анализ и оптимизация дизайн-систем

## Структура проекта

```
.
├── mcp_server/          # MCP сервер на Python
│   ├── server.py        # Основной MCP-сервер
│   └── tools/           # Инструменты для работы с Figma/LLM
├── figma_plugin/        # Figma плагин
│   ├── src/             # Исходный код
│   ├── ui/              # UI компоненты
│   └── styles/          # CSS стили
└── docs/               # Документация
```

## Установка и запуск

### MCP Сервер

```bash
cd mcp_server
pip install -r requirements.txt
python server.py
```

### Figma Plugin

```bash
cd figma_plugin
npm install
npm run build
```

## Использование

1. Запустите MCP сервер
2. Установите плагин в Figma
3. Подключитесь к серверу через плагин
4. Используйте AI-ассистента для работы с дизайном

## Дизайн-токены

В проекте используется система дизайн-токенов для поддержания единого стиля:

- Цвета (primary-color, secondary-color, etc.)
- Типографика (heading-font, body-font, etc.)
- Отступы (spacing-sm, spacing-md, etc.)
- Эффекты (shadow-sm, shadow-md, etc.)

## Разработка

### Требования

- Python 3.9+
- Node.js 16+
- Figma Desktop App

### Команды

```bash
# Запуск в режиме разработки
npm run dev

# Сборка проекта
npm run build

# Запуск тестов
npm test
```

## Статус проекта

Проект находится в активной разработке.

### Текущий прогресс
- ✅ Базовая инфраструктура
- ✅ Инструменты для работы с Figma
- ✅ Базовая AI интеграция
- ✅ Расширенные инструменты AI-анализа
- ✅ Генерация кода и адаптивных макетов
- ✅ Тестирование и документация
- ✅ Развертывание и практическое использование
- 🔄 Разработка Figma-плагина (в процессе)
- ⏳ Оптимизация и расширение (запланировано)

### Репозиторий и ветки

#### Структура веток

- **`main`** - основная ветка, содержит стабильный код
  - Текущее состояние: Базовая структура проекта и документация

- **`feature/mcp-server`** - ветка разработки MCP-сервера
  - Текущее состояние: MCP-сервер полностью разработан с интеграцией AI
  - Включает инструменты анализа дизайна и генерации кода

- **`feature/figma-plugin-development`** - ветка разработки плагина Figma
  - Текущее состояние: Начальная структура и базовый код плагина
  - Реализована базовая механика WebSocket-соединения

- **`dev`** - ветка для интеграции всех изменений перед слиянием с main
  - Текущее состояние: Синхронизирована с main

#### Порядок работы с ветками

Разработка новых функций выполняется в тематических ветках с префиксами `feature/`, `bugfix/` или `hotfix/`. После код-ревью и тестирования изменения интегрируются в `dev`, а затем в `main`.

### Документация

Полная документация доступна в директории `docs/`:
- [План разработки](docs/DEVELOPMENT_PLAN.md)
- [AI интеграция](docs/AI_INTEGRATION.md)
- [Руководство по разработке плагина](docs/FIGMA_PLUGIN_DEVELOPMENT.md)
- [Практические примеры](docs/PRACTICAL_EXAMPLES.md)
- [Руководство пользователя](docs/USER_GUIDE.md)

## Лицензия

MIT 

# Figma MCP интеграция

Интеграция Figma и MCP (Model Context Protocol) для автоматизации дизайна с помощью LLM.

## Решения проблем

### Проблемы с WebSocket соединением

Если вы видите ошибку при подключении к WebSocket серверу в логах плагина:
```
Соединение установлено успешно!
Соединение закрыто
```

Убедитесь, что:

1. В сервере MCP в файле `mcp_server/tools/websocket.py` метод `handler` имеет правильную сигнатуру:
```python
async def handler(self, websocket: websockets.WebSocketServerProtocol):
    # Обратите внимание: параметр path отсутствует
```

Эта ошибка проявляется как `TypeError: WebSocketServer.handler() missing 1 required positional argument: 'path'` в логах сервера и приводит к закрытию соединения с кодом 1011.

2. Порты в конфигурации совпадают:
   - В файле `.env`: `WEBSOCKET_PORT=8766`
   - В файле плагина `ui.html`: `const serverUrl = 'ws://localhost:8766';`

3. MCP-сервер запущен и доступен:
   ```bash
   cd mcp_server && python server.py
   ```

### Обработка сообщений WebSocket

Сервер обрабатывает следующие типы сообщений:
- `PING` - тестовый пинг для проверки соединения
- `ECHO` - эхо-запрос для тестирования двустороннего обмена данными
- `NODE_UPDATED` - сообщение об обновлении узла в Figma
- `NODE_CREATED` - сообщение о создании нового узла
- `NODE_DELETED` - сообщение об удалении узла
- `ERROR` - сообщение об ошибке

Для добавления новых типов сообщений используйте декоратор `@ws_server.register_handler()`:

```python
@ws_server.register_handler("NEW_MESSAGE_TYPE")
async def handle_new_message(websocket, payload):
    # Обработка нового типа сообщения
    pass
```

### Отладка WebSocket соединения

Для отладки WebSocket соединения можно использовать:

1. **Экспорт логов из плагина** - для этого нажмите кнопку "Экспорт логов" в интерфейсе плагина.

2. **Тестирование соединения** - запустите тестовый скрипт:
   ```bash
   node test_websocket.js
   ```

3. **Просмотр логов сервера** - в директории `mcp_server/logs/`:
   ```bash
   cat mcp_server/logs/mcp_server_*.log
   ```

## Компоненты

1. **MCP сервер** (`mcp_server/`) - Python сервер, обрабатывающий запросы от Figma-плагина и взаимодействующий с LLM.

2. **Figma плагин** (`figma_plugin/`) - TypeScript/JS плагин, который добавляет функциональность в Figma.

## Архитектура взаимодействия

```
┌─────────────┐      WebSocket       ┌────────────┐      HTTP      ┌──────────┐
│ Figma Plugin │◄───(порт 8766)─────►│ MCP Server │◄──────────────►│  OpenAI  │
└─────────────┘                      └────────────┘                └──────────┘
       │                                   │
       │ postMessage                       │
       ▼                                   ▼
┌─────────────┐                      ┌────────────┐
│ Plugin UI   │                      │ Figma API  │
└─────────────┘                      └────────────┘
```

## Билд и запуск

1. **MCP сервер**:
   ```bash
   cd mcp_server
   python server.py
   ```

2. **Figma плагин**:
   ```bash
   cd figma_plugin
   npm install
   npm run build
   ```
   После этого загрузите плагин в Figma из папки `figma_plugin/dist`. 