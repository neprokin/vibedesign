---
description: 
globs: 
alwaysApply: true
---
---
description: Guidelines for building a Python-based MCP server with a Figma plugin (WebSocket) and integrating LLM
globs: "**/*.py, **/*.ts, **/*.tsx"
---

# Building a Python-based MCP Server for Figma Automation with LLM

## Overview

В данном документе описаны обязательные правила (rule) для разработки:

1. **MCP-сервера на Python** (используя FastMCP и, при необходимости, FastAPI/Flask).
2. **Figma-плагина** на TypeScript/JavaScript для внесения изменений в дизайн (через WebSocket или postMessage).
3. **Интеграции с LLM** (OpenAI API или локальные модели) для генерации/редактирования кода.
4. **Cursor IDE** как основная среда, где AI обрабатывает команды (способствуя Multi-Choice Prompting и автоматизации).
5. (Опционально) **База данных** (Redis/PostgreSQL) для кэширования, и **CI/CD** (Docker, GitHub Actions, Kubernetes) для автоматического тестирования/развёртывания.

Все указанные пункты являются критическими для правильной настройки автоматизации Figma ↔ MCP ↔ LLM, а также для избегания типовых ошибок, ведущих к сбоям в проекте.

---

## ⚠️ CRITICAL INSTRUCTIONS FOR AI LANGUAGE MODELS ⚠️

В рамках этого продукта:

1. **Вы НЕ должны генерировать прямой вызов Figma REST API для записи** (редактирования дизайна).  
   - REST в Figma предназначен только для чтения.  
   - Любые операции записи (создание/удаление/обновление слоёв, смена цветов) выполняются через плагин.

2. **Вы НЕ должны хранить ключи (Figma, LLM, БД) в коде**  
   - Ключи, токены и другие секреты обязаны использоваться только из переменных окружения (или vault), и никогда не хардкодятся в репозитории.

3. **Вы НЕ должны напрямую использовать старые методы MCP (deprecated)**  
   - Если используете FastMCP, вы обязаны регистрировать инструменты через `@mcp.tool()` и обрабатывать исключения корректно.  
   - Не генерируйте устаревшие паттерны типа `@tool(name="...")` (если они помечены в вашей версии FastMCP как устаревшие).

4. **Вы НЕ должны генерировать неструктурированные пакеты данных в WebSocket-сообщениях**  
   - Каждое сообщение MCP ↔ плагин должно иметь JSON-формат: `{"type": "UPDATE_NODE", "payload": {...}}`.  
   - Избегайте произвольных строковых протоколов без поля `type`.

Нарушение любой из этих инструкций может привести к нестабильной работе приложения, утечкам данных, потере контекста в AI и некорректному редактированию дизайна в Figma.

---

## ABSOLUTE REQUIREMENTS FOR AI CODE GENERATION

1. **MCP-сервер (Python)**  
   - Всегда объявляйте инструменты через `@mcp.tool()` или `mcp.tool()(your_function)`.  
   - Чётко делите инструменты на «чтение» (REST Figma, LLM запросы) и «запись» (обращение к плагину) — не смешивайте их.  
   - Если необходимо REST-API, используйте FastAPI/Flask-эндпоинты в отдельном модуле, не путайте их с MCP-инструментами.

2. **Figma-плагин (TypeScript)**  
   - Для внесения изменений (Node fills, text content, перемещение слоёв) обязателен JSON-формат сообщений:  
     ```ts
     // ✅ ALWAYS:
     socket.send(JSON.stringify({
       type: "UPDATE_FILL",
       payload: {
         nodeId: "xxx",
         fillColor: { r: 1, g: 0.5, b: 0 }
       }
     }))
     ```
   - Никогда не отправляйте строковые команды без поля `type`:
     ```ts
     // ❌ NEVER:
     socket.send("updateFill:xxx:1,0.5,0")
     ```
   - При получении сообщений обязательно валидируйте `type` и `payload`.

3. **LLM-интеграция**  
   - Всегда используйте отдельный адаптер, например `llm_adapter.py`, с чёткой функцией `send_prompt(...)`.  
   - Никогда не смешивайте логику генерации кода с логикой обработки WebSocket-сообщений.  
   - Учитывайте контекст: передавайте в LLM только релевантные фрагменты JSON, не загружайте целый проект Figma, если нужна информация лишь о 1–2 слоях.

4. **Безопасность токенов**  
   - В Python не пишите `OPENAI_API_KEY = "..."` напрямую.  
   - Всегда берите ключи из окружения: `os.environ["OPENAI_API_KEY"]`.

---

## 🚨 DEPRECATED CODE PATTERNS 🚨

Любой код, содержащий следующие элементы, **НЕ должен** генерироваться или использоваться:

```python
# ❌ DEPRECATED:
from mcp.server.fastmcp import tool  # cтарая версия декоратора
@tool("some_tool")  # устаревший синтаксис, не поддерживается
def some_tool_func():
    pass

# ❌ Доступ к Figma API для записи:
import requests
requests.patch("https://api.figma.com/v1/files/xxx", ...)  # НЕЛЬЗЯ ИСПОЛЬЗОВАТЬ ПАТЧ